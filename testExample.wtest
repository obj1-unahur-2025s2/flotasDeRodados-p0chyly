import rodados.*

describe "Testeamos a los Corsa"{

const corsita = new Corsa(color = rojo)

test "su capacidad"{
  assert.equals(4, corsita.capacidad())
}

test "su velocidad"{
  assert.equals(150, corsita.velocidad())
}

}